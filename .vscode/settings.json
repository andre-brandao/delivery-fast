{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "server": "localhost",
            "driver": "PostgreSQL",
            "connectString": "postgres://root:mysecretpassword@localhost:5432/local?sslmode=disable",
            "name": "local"
        }
    ]
}